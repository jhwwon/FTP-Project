# FTP 클라이언트-서버 구현

C 언어로 구현한 완전한 FTP(File Transfer Protocol) 클라이언트-서버 시스템입니다. 네트워크 프로그래밍 프로젝트로 개발되었습니다.

## 📋 프로젝트 개요

이 프로젝트는 소켓 프로그래밍, 멀티스레딩, 멀티프로세싱, I/O 멀티플렉싱 등 다양한 네트워크 프로그래밍 개념을 보여주는 커스텀 FTP 프로토콜을 구현합니다.

## 🚀 주요 기능

### 지원하는 FTP 명령어
- **`ls`** - 서버 디렉토리 목록 조회
- **`cls`** - 클라이언트 로컬 디렉토리 목록 조회  
- **`cd`** - 서버 디렉토리 변경
- **`ccd`** - 클라이언트 로컬 디렉토리 변경
- **`get`** - 서버에서 클라이언트로 파일 다운로드
- **`put`** - 클라이언트에서 서버로 파일 업로드
- **`quit`** - 연결 종료 및 프로그램 종료
- **`help`** - 사용 가능한 명령어 표시

### 서버 구현 버전들
프로젝트는 다양한 동시성 모델을 보여주는 여러 서버 구현을 포함합니다:

1. **`srv.c`** - **멀티스레드 서버** (pthread 기반)
2. **`srv1.c`** - **단일 클라이언트 서버** (순차 처리)
3. **`srv2.c`** - **I/O 멀티플렉싱 서버** (select() 기반)
4. **`srv3.c`** - **멀티프로세스 서버** (fork() 기반)
5. **`srv4.c`** - **대체 멀티스레드 서버**

### 클라이언트 구현
- **`cli.c`** - 완전한 기능을 갖춘 FTP 클라이언트
- **`shell.c`** - 기본 클라이언트 구현
- **`testcli.c`** - 개발용 테스트 클라이언트

## 🛠️ 기술적 특징

### 네트워크 프로그래밍 개념
- **소켓 프로그래밍**: TCP 소켓 통신
- **멀티스레딩**: pthread 기반 동시 클라이언트 처리
- **I/O 멀티플렉싱**: select() 시스템 콜을 통한 효율적인 I/O
- **멀티프로세싱**: fork() 기반 프로세스 관리
- **파일 전송 프로토콜**: 오류 처리가 포함된 청크 기반 파일 전송

### 시스템 프로그래밍
- **프로세스 관리**: fork(), execvp(), wait()
- **파일 I/O**: open(), read(), write(), close()
- **디렉토리 관리**: chdir(), getcwd()
- **표준 입출력 리다이렉션**: stdout을 파일로 리다이렉션

## 📁 프로젝트 구조

```
ftp-project/
├── cli/                    # 클라이언트 구현
│   ├── cli.c              # 메인 클라이언트 프로그램
│   ├── shell.c            # 기본 클라이언트
│   ├── testcli.c          # 테스트 클라이언트
│   └── makefile           # 클라이언트 빌드 설정
├── srv/                    # 서버 구현
│   ├── srv.c              # 멀티스레드 서버
│   ├── srv1.c             # 단일 클라이언트 서버
│   ├── srv2.c             # I/O 멀티플렉싱 서버
│   ├── srv3.c             # 멀티프로세스 서버
│   ├── srv4.c             # 대체 멀티스레드 서버
│   └── makefile           # 서버 빌드 설정
├── *.txt                   # 테스트 파일들
└── README.md              # 이 파일
```

## 🔧 빌드 방법

### 필요 조건
- GCC 컴파일러
- pthread 라이브러리 (멀티스레드 서버용)
- Linux/Unix 환경 (또는 Windows의 WSL)

### 클라이언트 빌드
```bash
cd cli/
make
```

### 서버 빌드
```bash
cd srv/
make
```

### 수동 컴파일
```bash
# 클라이언트
gcc -o cli cli.c -lpthread

# 서버 (멀티스레드)
gcc -o srv srv.c -lpthread

# 서버 (I/O 멀티플렉싱)
gcc -o srv2 srv2.c -lpthread
```

## 🚀 사용 방법

### 서버 시작
```bash
./srv <포트번호>
# 예시: ./srv 8080
```

### 클라이언트 연결
```bash
./cli <포트번호>
# 예시: ./cli 8080
```

### 사용 예시
```
ftp>> help
-----------------------------------------------------------
| ls  [option] : list server directory        |
| cls [option] : list client directory        |
| cd  [option] : Change server directory      |
| ccd [option] : Change client directory      |
| get filename : Download a file from server  |
| put filename: Upload a file from client     |
| quit : exit                                 |
-----------------------------------------------------------

ftp>> ls
total 8
-rw-r--r-- 1 user user 1024 Dec 15 10:30 file1.txt
-rw-r--r-- 1 user user 2048 Dec 15 10:31 file2.txt

ftp>> get file1.txt
get started[get file1.txt]
count= 1
다운로드 완료

ftp>> put localfile.txt
put started[put localfile.txt]
file size=512,count=1
완료

ftp>> quit
종료 되었습니다
```

## 🔍 기술적 세부사항

### 파일 전송 프로토콜
- **청크 크기**: 65535 바이트 단위
- **상태 코드**: "OOOO" (성공), "XXXX" (실패)
- **파일 크기 정보**: 전송 전 파일 크기 및 청크 개수 교환

### 동시성 모델
1. **멀티스레딩**: 각 클라이언트를 별도 스레드로 처리
2. **멀티프로세싱**: 각 클라이언트를 별도 프로세스로 처리
3. **I/O 멀티플렉싱**: select()를 사용한 단일 프로세스 다중 클라이언트 처리
4. **순차 처리**: 한 번에 하나의 클라이언트만 처리

### 오류 처리
- 연결 오류 감지
- 파일 전송 오류 처리
- 잘못된 명령어 처리
- 클라이언트 연결 해제 감지

## 📚 학습 목표

이 프로젝트는 다음을 보여줍니다:
- 소켓 프로그래밍 기초
- 클라이언트-서버 아키텍처
- 동시성 프로그래밍 기법
- 파일 전송 프로토콜 설계
- 시스템 프로그래밍 개념
- 네트워크 프로그래밍 모범 사례

## 🎯 교육적 가치

다음 학습에 적합합니다:
- 네트워크 프로그래밍 개념
- 동시성 프로그래밍 모델
- 시스템 프로그래밍 기법
- 프로토콜 설계 및 구현
- 시스템 콜을 사용한 C 프로그래밍

## 📝 라이선스

이 프로젝트는 네트워크 프로그래밍 과목의 교육 목적으로 제작되었습니다.

## 👨‍💻 개발자

2022년 네트워크 프로그래밍 프로젝트로 개발되었습니다.

---

*이 구현은 실용적인 FTP 클라이언트-서버 애플리케이션을 통해 네트워크 프로그래밍 개념에 대한 포괄적인 이해를 제공합니다.*
